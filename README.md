## To run this application, type and run this command:

`npm i`

_or_

`npm install`

## Then run this command to run the server:

`npm run dev`

## Then open the localhost in the browser:

`localhost:3000`
